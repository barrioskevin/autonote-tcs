{
  "name": "autonote",
  "version": "1.0.0",
  "description": "automated note submitter",
  "scripts": {
    "compile": "tsc",
    "submit": "npm run compile && node submitter.js --env-file=.env",
    "generate": "npm run compile && node generator.js --env-file=.env",
    "test": "test"
  },
  "author": "kusa",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.7",
    "node": "^18.20.6",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "prettier": "3.5.3"
  }
}
